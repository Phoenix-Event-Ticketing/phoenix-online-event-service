services:
  event-service:
    build: .
    ports:
      - "4001:4001"
    env_file:
      - .env
    environment:
      PORT: 4001
    depends_on:
      - mongodb

  mongodb:
    image: mongo:7
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
